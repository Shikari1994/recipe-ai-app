name = "recipe-ai-proxy"
main = "worker.js"
compatibility_date = "2024-01-01"

# Переменные окружения (секреты)
# API ключ нужно будет добавить через: wrangler secret put OPENROUTER_API_KEY
[vars]
# Публичные переменные (если нужны)

# Для production окружения
[env.production]
name = "recipe-ai-proxy-production"
