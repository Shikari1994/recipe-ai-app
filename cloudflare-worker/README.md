# Cloudflare Worker для AI Recipe Assistant

Этот Cloudflare Worker проксирует запросы к OpenRouter API, скрывая API ключ от клиентов.

## 🚀 Быстрый старт

### 1. Установка Wrangler CLI

```bash
npm install -g wrangler
```

### 2. Авторизация в Cloudflare

```bash
wrangler login
```

Откроется браузер для входа в аккаунт Cloudflare.

### 3. Добавление API ключа

```bash
wrangler secret put OPENROUTER_API_KEY
```

Введите ваш OpenRouter API ключ когда появится запрос.

### 4. Деплой Worker

```bash
wrangler deploy
```

После деплоя вы получите URL вашего worker, например:
`https://recipe-ai-proxy.your-subdomain.workers.dev`

## 📝 Что дальше?

1. Скопируйте URL вашего worker
2. Обновите `constants/apiConfig.ts` в приложении:
   - Замените `OPENROUTER_BASE_URL` на URL вашего worker
   - Удалите `OPENROUTER_API_KEY` из клиентского кода

## 🔒 Безопасность

- ✅ API ключ хранится только на Cloudflare (недоступен из приложения)
- ✅ CORS настроен для защиты от несанкционированных запросов
- ✅ Ограничение размера запросов (500KB)
- ✅ Только POST запросы разрешены

## 💰 Бесплатные лимиты Cloudflare

- 100,000 запросов в день
- 10ms CPU time на запрос
- Этого достаточно для большинства приложений

## 🔧 Тестирование локально

```bash
wrangler dev
```

Worker запустится локально на `http://localhost:8787`

## 📊 Мониторинг

Смотрите логи и статистику в Cloudflare Dashboard:
https://dash.cloudflare.com/

## ⚠️ Важно

Не коммитьте `.env` файлы с API ключами в git!
