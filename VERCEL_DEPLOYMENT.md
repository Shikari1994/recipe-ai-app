# 🚀 Пошаговое развертывание на Vercel

## Что такое Vercel?

Vercel - это платформа для развертывания веб-приложений и Edge Functions. Отлично подходит для скрытия API ключей!

### Бесплатный тариф включает:
- ✅ **1 миллион запросов** в месяц (вместо 10k у Deno!)
- ✅ **100 GB** трафика
- ✅ **Неограниченное** CPU время
- ✅ Автоматический deploy при push в GitHub
- ✅ HTTPS из коробки
- ✅ Глобальная CDN

---

## Шаг 1: Создание аккаунта

1. Перейдите на [vercel.com](https://vercel.com)
2. Нажмите **Sign Up**
3. Выберите **Continue with GitHub** (это важно для автоматического deploy!)
4. Разрешите Vercel доступ к GitHub

---

## Шаг 2: Загрузка проекта на GitHub (если еще не сделано)

Если ваш проект еще не на GitHub:

```bash
# В корне проекта
git init
git add .
git commit -m "Initial commit with Vercel Edge Function"

# Создайте репозиторий на github.com, затем:
git remote add origin https://github.com/ваш-username/название-репо.git
git branch -M main
git push -u origin main
```

---

## Шаг 3: Импорт проекта в Vercel

### Вариант A: Через веб-интерфейс (проще)

1. На главной странице Vercel нажмите **Add New... → Project**
2. Найдите ваш репозиторий в списке
3. Нажмите **Import**
4. В настройках проекта:
   - **Framework Preset**: Other (или оставьте пустым)
   - **Root Directory**: `.` (корень проекта)
   - **Build Command**: оставьте пустым
   - **Output Directory**: оставьте пустым
5. Нажмите **Deploy**

### Вариант B: Через CLI

```bash
# Установите Vercel CLI
npm install -g vercel

# Войдите в аккаунт
vercel login

# Разверните проект
vercel
```

---

## Шаг 4: Добавление API ключа (КРИТИЧНО!)

После успешного развертывания:

1. Откройте ваш проект на Vercel
2. Перейдите в **Settings** (настройки)
3. Выберите **Environment Variables** (переменные окружения)
4. Нажмите **Add New**
5. Заполните:
   - **Key (Name)**: `OPENROUTER_API_KEY`
   - **Value**: ваш API ключ от OpenRouter (начинается с `sk-or-...`)
   - **Environments**: выберите **Production**, **Preview**, и **Development**
6. Нажмите **Save**

⚠️ **ВАЖНО**: После добавления переменной нужно сделать redeploy:
- Перейдите на вкладку **Deployments**
- Найдите последний deployment
- Нажмите на три точки `...` → **Redeploy**

---

## Шаг 5: Получение URL вашей Edge Function

После развертывания вы увидите URL вашего проекта, например:
```
https://myapp-abc123.vercel.app
```

Ваша Edge Function будет доступна по адресу:
```
https://myapp-abc123.vercel.app/api/openrouter-proxy
```

**Скопируйте этот полный URL!**

---

## Шаг 6: Обновление конфигурации приложения

1. Откройте файл `constants/apiConfig.ts`
2. Замените URL:

```typescript
export const API_CONFIG = {
  EDGE_FUNCTION_URL: 'https://myapp-abc123.vercel.app/api/openrouter-proxy',
  // Остальное без изменений...
};
```

3. Сохраните файл

---

## Шаг 7: Тестирование

### Тест 1: Проверка Edge Function через curl

Откройте терминал и выполните:

```bash
curl -X POST https://myapp-abc123.vercel.app/api/openrouter-proxy \
  -H "Content-Type: application/json" \
  -d "{\"model\":\"google/gemini-2.5-flash-lite\",\"messages\":[{\"role\":\"user\",\"content\":\"Привет!\"}],\"max_tokens\":100}"
```

Если все настроено правильно, вы получите JSON ответ от OpenRouter.

### Тест 2: Проверка в приложении

1. Запустите ваше приложение: `npm start`
2. Попробуйте найти рецепты
3. Проверьте, что запросы успешно обрабатываются

---

## Шаг 8: Автоматическое развертывание (уже настроено!)

Теперь при каждом `git push` в main ветку:
- Vercel автоматически обнаружит изменения
- Пересоберет и развернет Edge Function
- Обновит ваш проект

Никаких дополнительных команд не нужно! 🎉

---

## Проверка статуса и логов

### В веб-интерфейсе Vercel:

1. **Deployments** - история всех развертываний
2. **Analytics** - статистика запросов
3. **Logs** - логи в реальном времени (можно увидеть console.log из Edge Function)
4. **Settings** - настройки проекта и переменные окружения

### Через CLI:

```bash
# Посмотреть логи
vercel logs

# Список развертываний
vercel ls
```

---

## Troubleshooting (Решение проблем)

### ❌ Ошибка: "Server configuration error"

**Причина**: Не установлена переменная окружения `OPENROUTER_API_KEY`

**Решение**:
1. Проверьте Settings → Environment Variables
2. Убедитесь, что ключ называется точно `OPENROUTER_API_KEY`
3. Сделайте redeploy после добавления переменной

---

### ❌ Ошибка CORS в приложении

**Причина**: Неправильный URL или Edge Function не отвечает

**Решение**:
1. Проверьте, что URL в `apiConfig.ts` правильный (с `/api/openrouter-proxy` в конце)
2. Проверьте, что проект успешно развернут (зеленая галочка в Deployments)
3. Проверьте логи в Vercel на наличие ошибок

---

### ❌ Ошибка: "Method not allowed"

**Причина**: Неправильный метод запроса

**Решение**: Edge Function принимает только POST запросы

---

### ❌ Функция работает медленно

**Причина**: Холодный старт или медленный ответ от OpenRouter

**Решение**:
- Vercel Edge Functions имеют быстрый холодный старт (~50-100ms)
- Основное время - это ответ от OpenRouter (2-10 сек)
- Это нормально для AI запросов

---

## Мониторинг использования

Следите за лимитами бесплатного тарифа:

1. Откройте **Settings → Usage**
2. Проверяйте:
   - **Edge Requests** (должно быть < 1 млн/месяц)
   - **Bandwidth** (должно быть < 100 GB/месяц)

---

## Следующие шаги

1. ✅ Протестируйте приложение с реальными запросами
2. ✅ Проверьте работу с фото продуктов
3. ✅ Следите за логами в Vercel
4. ✅ При необходимости настройте custom domain (Settings → Domains)

---

## Полезные ссылки

- [Vercel Documentation](https://vercel.com/docs)
- [Edge Functions Guide](https://vercel.com/docs/functions/edge-functions)
- [Environment Variables](https://vercel.com/docs/projects/environment-variables)

---

## Нужна помощь?

Если что-то не работает:
1. Проверьте логи в Vercel (вкладка Logs)
2. Убедитесь, что переменная окружения установлена
3. Проверьте URL в `apiConfig.ts`
4. Сделайте redeploy после изменений

Удачи! 🚀
